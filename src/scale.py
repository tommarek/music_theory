#!/usr/bin/env python3

#from scale_generator import ScaleGenerator

class Scale():
    def __init__(root='C', generator=None, notes=None):
        """
        Scale can be generated by a generator (well-formed scales) or using
        a list of notes.
        """
        if notes:
            self.notes = notes
        elif generator:
            self.notes = generator(root)
